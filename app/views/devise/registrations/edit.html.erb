<div class="page-header page-header-sm" data-parallax="false">
  <div class="filter"></div>
  <div class="container">
    <div class="motto text-center">
      <h1 id="head">Dronnie</h1>
    </div>
  </div>
</div>
<br>
<div class="container-fluid">
  <div class="mx-auto col-md-12 card card-new ">
    <h1 class="text-center">Edit User</h1>
    <div class="col-md-12 card list">
  <p class="text-center">User Name:
    <%= @user.first_name %>
  </p>
  <p class="text-center">
    Email:
    <%= @user.email %>
  </p>
  <p class="text-center">
    Last Visit:
    <%= @user.last_sign_in_at.strftime("%d/%m/%Y %I:%M %p") %>
  </p>
  <p class="text-center">
    Registration Date:
    <%= @user.created_at.strftime("%d/%m/%Y")%>
  </p>
  <p class="text-center">
    <%= link_to 'Edit', edit_user_path(@user), :class => "btn btn-warning btn-link btn-lg" %>
    <%= link_to 'Destroy', @user, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger btn-link btn-lg" %>
  </p>
</div>

    <%= link_to 'Show', @user, :class => "btn btn-info btn-link btn-lg" %>
    <%= link_to 'Back', users_path, :class => "btn btn-primary btn-link btn-lg" %>
  </div>
</div>
